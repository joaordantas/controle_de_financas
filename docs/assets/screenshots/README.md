# Screenshots públicos da Nivra

Diretório reservado para capturas revisadas da aplicação:

- `dashboard.png`;
- `transactions.png`;
- `accounts.png`;
- `cards.png`;
- `mobile.png`.

Antes de adicionar uma imagem, remova nomes, e-mails, hashes, saldos pessoais, credenciais e qualquer outro dado sensível. O README principal só deve referenciar arquivos que realmente existam.
